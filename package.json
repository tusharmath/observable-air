{
  "name": "observable-air",
  "version": "1.0.0",
  "description": "Observables for the calorie conscious",
  "main": "src/Observable",
  "scripts": {
    "test": "ava",
    "hydra": "node --trace-hydrogen --trace-phase=Z --trace-deopt --code-comments --hydrogen-track-positions --redirect-code-traces --redirect-code-traces-to=code.asm lib/benchmark.js",
    "rfc": "node chore/rfc",
    "benchmark": "node chore/benchmark > benchmark.md",
    "postinstall": "tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ava": "^0.16.0",
    "benchmark": "^2.1.1",
    "es-observable-tests": "^0.3.0",
    "typescript": "^2.0.3"
  }
}
