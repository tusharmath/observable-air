{
  "name": "observable-air",
  "version": "1.0.0",
  "description": "Observables for the calorie conscious",
  "main": "src/Observable",
  "scripts": {
    "test": "ava",
    "hydra": "node --trace-hydrogen --trace-phase=Z --trace-deopt --code-comments --hydrogen-track-positions --redirect-code-traces --redirect-code-traces-to=code.asm lib/profile.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ava": "^0.16.0"
  }
}
